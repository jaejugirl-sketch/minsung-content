# Orca 첫 실행 가이드

목표: 시드 하나로 **각도 2개 병렬 초안 → 비교 → 검수 → 사람 승인 → 병합·발행**까지 한 바퀴를 돈다.
이 최소 루프가 돌면 예약 자동화와 영문판은 그 위에 얹으면 된다.

## 0. 준비 (한 번만)
1. Claude Code 설치·로그인이 되어 있어야 한다(`npm i -g @anthropic-ai/claude-code` 후 한 번 로그인). Orca는 `~/.claude`를 자동으로 잡는다.
2. 이 폴더를 git 저장소로: 폴더에서 `git init && git add -A && git commit -m "content engine scaffold"`.
3. Orca를 열어 이 저장소를 연다.

## 1. 시드 하나 고르기
`시드/seeds.md`에서 이번에 쓸 씨앗 하나를 정한다. 없으면 지금 하나 적는다(관찰 한 줄 + 나올 글 한 줄).

## 2. 각도 뽑기 (게이트 ①)
worktree 하나에서 Claude Code를 띄우고: "`앵글기획관`으로 이 시드에 대해 각도 3개를 뽑아줘" — 시드 내용을 붙인다.
→ 나온 3개 중 **밀고 갈 각도를 직접 고른다.** (여기가 사람 게이트 ①)

## 3. 팬아웃 초안 (병렬 레이스)
같은 시드를 두 갈래로 동시에 뽑는다.
- Orca에서 **작업 2개를 만들어 각각 격리된 worktree/브랜치**로 띄운다(agent 콤보박스에서 Claude Code 선택).
- worktree A: "`초안작성관`으로 [각도 1]로 링크드인 초안"
- worktree B: "`초안작성관`으로 [각도 2]로 초안" (또는 같은 각도를 다른 문체로)
- 두 초안이 동시에 만들어진다. 급하지 않은 콘텐츠이므로 2개면 충분하다(병렬 N개 ≈ 사용량 N배).

## 4. 비교 → 검수
- Orca에서 두 초안을 **나란히 비교**해 이긴 쪽을 고른다.
- 그 worktree에서: "`보이스검수관`으로 이 초안 점검해줘" → 파일 상단에 검수 지적이 붙는다.

## 5. 사람 최종 게이트 (게이트 ②, 생략 불가)
검수 지적을 참고해 **필자가 직접 마지막 목소리를 얹고** 발행 여부를 결정한다. 이 단계는 어떤 경우에도 건너뛰지 않는다.

## 6. 병합·발행·축적
- 이긴 브랜치를 main에 **병합**하고, 완성본을 `발행/`으로 옮긴다.
- 블로그·챕터급이면 `책/책맵.md`에 주제 태그와 함께 기록한다.

## 다음 단계 (루프가 익으면)
- **예약 자동화:** `동향수집관`을 Orca의 크론 방식 자동화로 매주 월요일 실행 → 시드 뱅크 자동 충전.
- **영문판 브랜치:** 발행한 동향·제언 글에 `영문판` 스킬로 영어판을 붙인다.
- **오케스트레이션:** 익숙해지면 Orca orchestration 스킬로 2~5단계를 하나의 DAG로 묶되, 게이트 ②는 decision gate로 남긴다.

## 주의
- 팬아웃은 고부가 글에만. 짧은 링크드인까지 3중으로 돌리지 않는다.
- Orca는 코딩 에이전트용이라 브라우저 요소 선택기·시뮬레이터 등은 무시한다. worktree 병렬·비교/병합·예약 자동화·오케스트레이션 게이트만 쓰면 된다.
- 목표는 더 많이 쓰는 게 아니라 발행을 싸게 만드는 것.
