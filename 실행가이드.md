# 실행 가이드 (Claude Code 루틴 기준)

이 저장소는 두 갈래로 돈다.
- **자동:** 재료 수집(`동향수집관`)은 클라우드 루틴이 매일 알아서 돌려 시드 뱅크를 채우고, 결과를 이메일로 보낸다.
- **수동:** 시드를 실제 글로 키우는 일(앵글기획 → 초안 → 검수 → 발행)은 사람이 직접 세션을 열어 진행한다. 마지막 목소리와 발행 판단은 사람 몫이다.

두 갈래를 섞지 않는 것이 이 저장소 운영의 핵심이다.

---

## A. 자동 — 동향수집 루틴 (한 번 설정하면 매일 자동)

### 준비 (한 번만)
1. 이 저장소가 GitHub에 올라가 있어야 한다(원격 저장소 필요). 클라우드 루틴은 로컬 폴더가 아니라 GitHub 저장소를 클론해 실행한다.
2. `claude.ai/code/routines`에서 새 루틴(Cloud)을 만들고 이 저장소를 연결한다.

### 루틴 설정
- **트리거:** 매일 원하는 시각(예: 오전 9시).
- **커넥터:** Gmail만 켠다. (이메일 발송 권한. 안 쓰는 커넥터는 빼둔다.)
- **지침(프롬프트) 예시:**
  > 동향수집관 에이전트로 오늘 한국문학 해외진출 동향을 웹에서 훑어서 `시드/seeds.md`에 새 항목만 추가하라. 이미 seeds.md에 있는 항목과 중복되면 넣지 마라.
  >
  > 그다음 오늘 새로 추가한 항목만 따로 정리해서 [내 이메일]로 Gmail을 통해 보내라. 각 항목은 사실 한 줄 + 나올 수 있는 글 한 줄 + 출처 링크로 정리한다.
  >
  > 작업이 끝나면 변경사항을 git으로 커밋하고 main 브랜치에 push하라.
  >
  > 절대 앵글기획이나 초안 작성 단계로 넘어가지 마라. 재료 수집·이메일 발송·push까지만 하고 멈춰라.

### 왜 push가 필요한가
루틴은 실행마다 저장소를 새로 클론해 임시 공간에서 돌리고, 끝나면 임시 공간을 버린다. seeds.md에 추가한 내용을 커밋·push하지 않으면 실행이 끝나는 순간 사라진다. push해야 다음 날 중복 없이 이어지고, 사람이 세션을 열 때도 그동안 쌓인 시드가 보인다.

### 첫 실행 후 점검
1. 메일이 원하는 형식으로 도착했는가.
2. GitHub의 `시드/seeds.md`에 새 항목이 실제로 커밋됐는가.
3. 앵글기획·초안 단계로 넘어가지 않고 재료 수집에서 멈췄는가.

---

## B. 수동 — 시드 하나를 발행까지 (사람이 직접)

시드가 쌓이면, 쓸 마음이 든 날 세션을 하나 열어 아래 한 바퀴를 돈다. 지속 가능한 리듬(링크드인 주 1, 블로그 격주 1)이면 충분하다.

### 1. 시드 하나 고르기
`시드/seeds.md`에서 이번에 쓸 씨앗 하나를 정한다.

### 2. 각도 뽑기 (사람 게이트 ①)
세션에서: "`앵글기획관`으로 이 시드에 대해 각도 3개를 뽑아줘" — 시드 내용을 붙인다.
→ 나온 3개 중 **밀고 갈 각도를 직접 고른다.**

### 3. 초안
"`초안작성관`으로 [고른 각도]로 링크드인(또는 블로그) 초안" 을 시킨다.
- 고부가 글이면 각도 2개를 각각 다른 세션/브랜치에서 뽑아 나란히 비교하고 이긴 쪽을 고른다. 짧은 링크드인까지 이중으로 돌리지 않는다(병렬 N개 ≈ 사용량 N배).

### 4. 검수
"`보이스검수관`으로 이 초안 점검해줘" → 기관 화법·AI 흔적을 짚어준다. 검수관은 승인하지 않는다. 문제만 표시한다.

### 5. 사람 최종 게이트 (게이트 ②, 생략 불가)
검수 지적을 참고해 **필자가 직접 마지막 목소리를 얹고** 발행 여부를 결정한다. 이 단계는 어떤 경우에도 건너뛰지 않는다.

### 6. 발행·축적
- 완성본을 `발행/`으로 옮긴다.
- 블로그·챕터급이면 `책/책맵.md`에 주제 태그와 함께 기록한다.
- 발행한 동향·제언 글에는 `영문판` 스킬로 영어판을 붙일 수 있다.

---

## 주의
- 자동 루틴은 재료 수집까지만. 글쓰기·발행은 언제나 사람이 연 세션에서.
- 게이트 ②(사람 최종 판단)는 어떤 경우에도 건너뛰지 않는다. 이 분야 신뢰는 여기서 지켜진다.
- 목표는 더 많이 쓰는 게 아니라 "발행"을 싸게 만드는 것.
